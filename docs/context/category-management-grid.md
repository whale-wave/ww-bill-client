# 分类管理子分类宫格调整

更新日期：2026-09-23。状态：设置态与记账选择态实现及本地验证完成，实体设备待用户加载新版确认。

## 用户要求

参考友商：一级分类下方的每个子分类只显示图标与名称，不接受子分类卡片内常驻“隐藏/前移”的展示。

## 调整与决策

| Before | After | Why |
| --- | --- | --- |
| 三列带独立底色的大子卡片 | 常规五列、360px 以下四列紧凑宫格，单项仅圆形图标和名称 | 匹配参考页面的密度和层次 |
| 单独二级分类标题与数量行 | 一级图标/名称区域提供折叠箭头 | 减少一层冗余标题 |
| 子项常驻隐藏、前移 | 点击子项进入编辑，编辑弹层保留操作 | 列表保持简洁，同时保留管理能力 |
| 宽大的“添加二级分类” | 宫格末尾同尺寸加号与“添加” | 与子项对齐 |

折叠遵循应用动态效果开关；只读用户可折叠浏览，无新增或编辑操作。长名称自然换行，图标底框等宽高，保留至少 44px 点击宽度。编辑里移动或隐藏后关闭弹层，避免排序更新版本后继续使用旧编辑状态。

## 实施过程

- 修改 CategoryManagement 父级折叠入口、五列子级宫格及编辑操作入口。
- 更新 DESIGN.md 并增加 EditableHierarchy 预览，包含多行子分类、空父级和末尾添加。
- 补充行为回归：各父级独立折叠、子项入口、编辑内同级前移与隐藏、添加时父级传参。

## 验证与交付

- 分类管理相关回归：2 文件、13 项通过，覆盖折叠、编辑、隐藏、同级前移、父级新增及已有图片流程。
- TypeScript、Storybook 类型检查、生产构建、静态 Storybook 构建通过；生产构建保留现有大包警告。
- 浏览器中查看可编辑宫格：多行图标/名称与末尾添加对齐，单独标题及子项隐藏/前移按钮不再出现；操作父级折叠、展开及子级编辑后，所需按钮均可达。
- 设置 390×844 与 320×740 预览；浏览器缩放使 DOM 实际宽度分别为 433px 与 355px，均无横向溢出，窄视口观察时最小子项宽度约 46px。另为 CSS 360px 以下加入四列回退，重建后浏览器确认 355px 下显示四列、最小子项宽度约 58.6px 且无横向溢出；未将预览视为实体设备验收。
- 已更新 DESIGN.md、文档索引与 Storybook `Features/Classification/EditableHierarchy`。没有修改二级分类数据模型或服务端接口；并行键盘样式改动保持独立。

## 2026-09-23 记账单屏补充

- 记账编辑器取消“分类页 → 二级分类页 → 金额页”的内容切换，分类、金额、备注和数字键盘保持在同一页面。
- 一级分类改为五列紧凑选择网格；带子级的父级显示三点角标，点击后在所在行下展开。展开区首项用于父级直记，后续项选择二级；选择完成后收起并在金额标题显示完整路径。
- 分类设置从分类内容区的大卡片移到顶部 44px 图标入口。分类加载、失败和空状态只占分类滚动区，不卸载金额与键盘；没有有效分类时完成按钮禁用。
- 设置页一级行新增二级数量说明，所有分组默认折叠并允许独立展开；新增、编辑或移动子级成功后重新展开并定位到对应父级。
- Storybook 增加单屏层级、展开子级、已选子级和纯一级分类预览；自动化覆盖键盘常驻、父级直记、二级选择、完整路径及设置态折叠。
- Storybook 在 320×568、375×667、390×844 下完成视觉复核：分类区内部滚动且展开前后键盘坐标不变，无横向溢出；分类、设置、完成及数字键等触控目标均不小于 44px。

## 2026-09-23 键盘上方紧凑输入补充

- 账户、标签、位置、日期和图片改为键盘上方同一条可横向滚动的 `44px` 入口，不更改对应选择弹层和草稿字段。
- 备注、所选分类路径及金额合并到键盘上方 `62px` 单行卡片；金额以紧凑字号展示，不再占用独立大块区域。加减运算移入键盘操作行，完成键保留原业务行为。短屏展开二级后分类区自动滚到展开组，不推动键盘。
- Storybook `CompactDetails` 展示已选二级分类、标签及金额；紧凑入口选中态仍使用文字和颜色共同表达。

## 2026-09-23 分类区铺满与悬浮属性入口

- 目标：金额已收敛到键盘上方后，分类区接管其余全部可用高度；属性标签悬浮在分类区底部，标签之间保持透明，能看到后方的分类界面。
- 范围：只调整记账编辑器的层级与滚动布局，不改分类、标签、位置等选择行为或草稿数据。
- 实施：分类区与浮动入口共用一个弹性容器；分类滚动内容增加底部余量，二级展开滚动定位避开悬浮入口；备注历史浮在分类区内，不推动金额或键盘。
- 验收：320×568、375×667、390×844 中分类区延伸至金额行，属性标签视觉叠放其上、缝隙透出分类区；分类末项、二级选项可滚动到标签上方，键盘不位移、无横向溢出。
- 实际结果：`RecordEditorPresentation.tsx` 将分类、备注历史和属性入口收进同一个弹性舞台；`record-editor-presentation.scss` 去掉分类固定高度。Storybook 新增 `FloatingDetailsOverCategories`，用多分类校验悬浮层级。
- 视觉证据：在 320×568、375×667、390×844 下，分类区底边与金额行顶边重合，属性入口位于分类区内部；三种尺寸均无页面横向溢出，分类末项滚到浮层上方且键盘贴底。390×844 的胶囊间隙命中后方分类图标，手指在胶囊上横滑仍可滚动入口条。
- 验证：生产构建、Storybook 构建与类型检查、全库 ESLint 均通过；完整 Vitest 回归 214 个文件、1236 个用例通过。真实设备与发布未验证。
- 状态：本地实现与验收完成，实现基于 `ffc01a5e`。
