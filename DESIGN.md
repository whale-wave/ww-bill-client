# Whale Wave Bill Design Guide

## 1. 产品气质

Whale Wave Bill 是面向移动端的个人账本/财务工具。界面首先服务于高频记账、账单查看、预算管理、资产汇总和图表分析，整体应保持轻量、清爽、可快速扫读。

设计方向不是营销站、桌面后台或重装饰 Dashboard，而是手机 App 工具界面：顶部呈现当前上下文和核心金额，中部承载列表、表单或数据模块，底部保留主导航或固定操作区。视觉表达要克制，重点让金额、日期、分类、预算进度、账单状态和操作入口一眼可见。

## 2. 视觉来源与基准节点

Figma Design 文件 `Whale Wave Bill New UI` 是新版界面的唯一视觉基准。页面实现使用 375px 内容宽度，不包含 Figma PhoneShell 的 9px 设备边框、系统状态栏和灵动岛。

- 明细：`1:6`
- 图表：`1:321`
- 记账分类：`1:992`
- 记账金额：`1:1629`
- 发现：`1:1738`
- 我的：`1:1949`
- 年月 Sheet：`1:2488`

新增页面和组件应优先复用当前项目已有样式入口，不另起一套设计系统。

- `src/assets/styles/reset.ts` 定义项目主色 `--ww-theme-color`，当前值为 `#6FC2DC`。
- `src/assets/styles/global.scss` 定义全局页面结构、Ant Design Mobile 变量映射、基础文本色和背景色。
- `tailwind.config.js` 定义项目字体栈、`bg-primary`、`text-primary`、`bg-bg-gray`、`text-font-black`、`text-font-gray`、`border-primary` 等常用 Tailwind token。
- `src/shared/ui/bottom-tab-bar/` 是个人、家庭与自定义账本共用的主导航展示层。
- `src/components/NavBar/` 和 Ant Design Mobile `NavBar` 是顶部导航的主要来源。
- `src/components/ui/*` 是较早的基础组件层。新增常规业务页面优先使用 Ant Design Mobile 和现有业务组件；只有与既有页面保持一致时才继续扩展该基础组件层。

## 3. 色彩

色彩以 Figma Make Version 10 为全局基准。业务组件优先使用 CSS 变量或 Tailwind token，语义色可在局部明确使用。

- **页面渐变**：`linear-gradient(154.699deg, #E2F6FF 6.9018%, #F4FBFF 50%, #FFF2F7 93.098%)`，通过 `--ww-page-gradient` 使用。
- **主色阶**：`#6FC2DC`、`#C8EAF6`、`#4AAAC4`、`#2B8BAA`，对应 `primary`、`primary-light`、`primary-mid`、`primary-deep`。
- **粉色阶**：`#F0A0B8`、`#FFD8E6`，对应 `ww-pink`、`ww-pink-light`。
- **内容表面**：默认使用 `rgba(255,255,255,0.84)`，实色白用于输入区和需要更高对比度的控件。
- **文本阶**：`#263340`、`#5C7080`、`#9BAEBB`、`#C4D4DC`，对应 `ww-ink`、`ww-mid`、`ww-soft`、`ww-ghost`。
- **描边**：浅蓝 `rgba(110,194,220,0.2)`，通过 `border-primary` 使用。
- **财务语义色**：收入、支出、风险、到期提醒和图表辅助色可以少量使用绿色、红色、橙色等语义色。它们只用于状态和数据含义，不替代主色。

避免大面积高饱和配色、霓虹发光、复杂纹理和无业务含义的装饰色块。预算、资产等摘要卡可以使用设计系统内的粉色或浅紫柔和渐变。

## 4. 字体与数字层级

项目通过 Fontsource 在构建产物中本地托管可变字体，不发起运行时远程字体请求。

- **默认字体**：`Noto Sans SC`，覆盖 400–900 字重；系统中文字体仅作为加载失败时的 fallback。
- **展示字体**：`font-display` 可用于启动页 Logo、少量品牌展示或较突出的页面标题，不用于密集列表正文。
- **数字字体**：金额、预算、资产、日期和百分比使用 `Nunito`，覆盖 400–900 字重。
- **常规正文**：多使用 `15px`、`16px`。
- **辅助信息**：多使用 `12px`、`13px`、`14px`。
- **页面标题/顶部 Tab**：多使用 `17px`、`18px`，重要标题可到 `20px`、`22px`。
- **金额层级**：列表金额约 `16px`，卡片金额可用 `18px`、`24px`，总额或净资产可用 `30px` 到 `36px` 并加粗。

中文正文不使用负字距。文本溢出优先使用 `.one-line` 或等价省略处理，避免撑破手机宽度。

## 5. 页面结构与间距

页面按移动端 App 组织，优先复用 `.page` 或 `.page-new`。

- 根容器保持 `height: 100%` 和纵向 flex。需要内部滚动时，把滚动放在中间内容区。
- 顶部优先参与正常文档流，只有确需固定时才让内容区显式避让。
- 主 TabBar 不参与文档流，内容可从半透明底栏后方滚动；对应滚动容器使用 `.ww-tab-bar-scroll-padding`，预留 `126px + safe-area-inset-bottom`（66px 底栏、12px 底部偏移和 48px 末尾留白）。
- 375px 基准下，页面模块左右留白通常为 `18px`，标题栏为 `22px`，记账网格为 `14px`。
- 模块之间的垂直间距以 `8px`、`10px`、`12px`、`14px`、`16px` 为主。
- 信息列表优先使用白底行项目和细分割线，而不是把每一行做成厚重卡片。

顶部数据区可以使用主色背景承载月份、收入、支出、结余等核心信息；中部列表保持白色可读；底部导航保持稳定，不因页面内容变化跳动。

## 6. 圆角、阴影与容器

Version 10 使用柔和的大圆角卡片和两级浅阴影，但不使用厚重投影。

- **通用业务卡片**：使用 `20px`；五宫格菜单和分类卡使用 `18px`。
- **强调容器/Sheet**：使用 `24px`；浮动底栏使用 `33px`。
- **小控件/标签/分段控制**：使用 `13px` 到完全胶囊圆角。
- **圆形元素**：只用于分类图标、头像、TabBar 中间新增按钮、进度环等明确圆形元素。
- **阴影**：`shadow-ww-xs` 为小控件，`shadow-ww` 为普通卡片，`shadow-ww-lg` 为汇总卡，`shadow-ww-floating` 为底栏。列表行自身不用阴影。

共享展示优先使用 `GradientPanel`、`MetricGrid`、`ActionMenuCard`、`SettingsListCard`，避免在页面中重复拼接相同的圆角、描边和阴影。

## 7. 核心组件模式

### 顶部导航

常规页面优先使用 `src/components/NavBar/NavBar.tsx` 或 Ant Design Mobile `NavBar`。主色顶部栏用于账本、预算、图表等核心流程；资产、设置等工具页可使用白底顶部栏，但高度、字号和返回入口要与现有页面接近。

### 底部导航

主 TabBar 使用 fixed 定位且不占据页面布局；在 375px 下左右各 `14px`、距安全区上方 `12px`、高 `66px`、圆角 `33px`。中间“记账”入口为 `52px` 主色渐变圆形按钮。滚动内容可以透过半透明底栏被看见，但末项必须能滚动到导航上方。

### 列表

账单、预算、资产、发票和设置类页面以列表为主要信息载体。列表行高度通常在 `55px`、`59px`、`60px`、`74px` 附近；主信息靠左，金额或状态靠右；辅助信息使用灰色小字号。分割线优先放在内容区域侧，避免整屏粗边框。

### 卡片

卡片用于汇总信息、当前月份账单、预算概览、资产概览和功能入口。渐变摘要卡内部可包含轻量白色快捷 Action tile；普通列表仍以单层白色分组卡为主。

### 表单

表单、弹窗、Toast、List、Tabs、Dropdown、SearchBar 优先使用 Ant Design Mobile。输入行保持清晰标签、足够触控区域和稳定分割线。表单及页面级操作统一使用原生 `button` 的按钮规范，不再使用 Ant Design Mobile `Button` 作为主操作：

- **主按钮**：`h-[52px] w-full rounded-[18px] border-0 bg-primary text-[14px] font-extrabold text-white shadow-ww disabled:opacity-45`；提交中同时切换 `disabled` 与 loading 文案。
- **次按钮**：`h-[52px] w-full rounded-[18px] border border-solid border-border-primary bg-white/85 text-[14px] font-extrabold text-primary-deep shadow-ww`。
- **幽灵按钮**：`h-11 w-full border-0 bg-transparent text-[12px] font-extrabold text-primary-deep`，danger 文案使用 `text-[#b24f71]`，需要时补充 `disabled:opacity-45`。
- **小按钮**：`h-12 w-full rounded-[16px] text-[13px] font-extrabold`；主变体 `border-0 bg-primary text-white shadow-ww-xs`，次变体 `border border-solid border-border-primary bg-white/85 text-primary-deep shadow-ww-xs`。

非提交按钮必须显式 `type="button"`，避免原生 `button` 的默认 submit 语义。`IllustratedEmptyState` 等封装组件的组件级 CTA 保留组件自身 pill 样式，不在上述规范约束范围内。

### 图标

七状态中的页面级图标统一从 `src/assets/icons/figma/` 读取本地 Figma 导出文件，并通过类型化 `DesignIcon` 渲染；不得用 iconfont 或形状相近的通用图标替换。底栏普通图标为 `19px`，中央记账图标为 `22px`。

分类图标统一由 `CategoryIcon` 渲染，内置图标映射到 Lucide，`strokeWidth` 固定为 `1.8`：列表 `18px`、排行 `16px`、记账分类 `24px`。服务端默认分类键必须有明确映射；未知的系统内置键可以按名称推断，最终回退 `ReceiptText`。七状态及家庭/自定义账本的共享消费者不得再依赖阿里 iconfont。

自定义图片图标使用 1:1 方形缩略图和 `object-fit: cover`，在服务端统一生成 256×256 WebP。列表及排行随对应内置图标底框使用约 24% 圆角，不使用圆形头像语义；记账网格按分类卡的图标底尺寸放大，但保持同一裁切比例。图片跨域加载统一使用匿名 CORS，加载失败直接回退 `ReceiptText`，不得再按自定义分类名称猜测图标。家庭预算快照、历史记录、预算、图表、搜索和分享海报都复用 `CategoryIcon` 的尺寸、圆角与失败回退规则。

## 7.1 七状态计算后尺寸

- **明细 `1:6`**：标题栏横向 `22px`，标题 `17/25.5 Bold`，操作按钮 `36px`；汇总卡宽 `calc(100% - 36px)`、375px 下 `339×229.25px`、圆角 `20px`、内边距 `22px`。快捷入口为 `91×70px`、圆角 `14px`；记录行 `64px`，单记录卡总高 `70px`，分隔线从左侧 `71px` 起。
- **图表 `1:321`**：分段控件宽 `339px`、圆角 `14px`；汇总卡 `339×212.5px`、内边距 `20px`；指标区 `62.5px`，主金额 `28/42 Nunito Black`；绘图区 `315×94px`；排行图标底 `34px`、行高 `56px`。
- **记账分类 `1:992`**：页面横向 `14px`，四列 `80px`，列/行间距 `9px`；分类卡 `80×92.5px`、圆角 `18px`、图标底 `44px`。
- **记账金额 `1:1629`**：主内容横向 `22px`；返回按钮 `40px`；备注卡 `50px`、圆角 `14px`；金额 `54/81 Nunito Black`；日期和完成按钮 `50px`、圆角 `16px`；数字键三列、间距 `8px`。
- **发现 `1:1738`**：页面横向 `18px`；账单、预算、资产卡圆角 `20px`、水平内边距 `20px`、垂直内边距 `18px`；功能 Tile 基准宽 `106px`、高 `75px`（内容核心高 `70px`）、圆角 `16px`。
- **我的 `1:1949`**：顶部卡横向 `18px`、内边距 `22×24px`；头像 `68px`；菜单图标底 `36px`；五宫格行高 `74px`；设置行 `47px`。
- **年月 Sheet `1:2488`**：遮罩 `rgba(30,50,70,.35)`；面板顶部圆角 `24px`，阴影 `0 -8px 32px rgba(40,100,150,.16)`；年份和月份均为四列，按钮高分别为 `34px`、`42px`，间距 `8px`。

### 图表

图表页面以解释账单趋势和分类占比为目标。坐标轴、网格线和背景要轻；当前选中数据、排行数字和业务结论要突出。ECharts 配色应与主色、灰阶和少量语义色协调。

## 8. 交互与状态

交互反馈应直接、轻量，不做复杂动效。

- 点击列表项进入详情时可保留现有翻页音效模式。
- 激活态使用主色、浅青底或更明确的文字权重。
- 禁用态和等待态使用灰色文本或低对比背景，不要使用强红或强蓝。
- 加载、空状态和错误状态优先使用 Ant Design Mobile 现有组件，再做局部样式适配。
- 固定底部操作区要保证不遮挡列表最后一项，内容区必须预留安全空间。

动画只用于必要的状态变化和页面反馈。不要新增大范围视差、浮动装饰、循环动效或影响记账效率的过渡。

## 9. 页面类型规范

- **明细/账单页**：顶部突出月份和收支结余，中部按日期分组，列表项展示分类、备注和金额。
- **记账页**：输入过程要短路径、强触控目标、键盘/分类/金额区域层级明确。
- **预算页**：优先展示剩余金额、预算进度和分类预算；进度关系要比装饰更重要。
- **资产页**：总资产/负债/净资产优先级最高，账户列表保持可比较的金额排布。
- **图表页**：筛选入口稳定，图表和排行榜互相解释，不堆砌图形。
- **我的/设置页**：白底列表和清晰分组为主，减少多余视觉强调。
- **社区/分享类页面**：可以比账本页稍微活泼，但仍保持项目灰阶、主色和小圆角体系。

## 10. 禁止事项

- 不要新增设计 token 之外的高饱和渐变、霓虹光、强 3D、厚重投影、大插画背景或营销页 Hero。
- 不要把移动端页面做成桌面后台布局。
- 不要绕过 `--ww-theme-color`、Tailwind 主题色或现有 Sass 结构另起品牌色。
- 不要随意修改全局 Ant Design Mobile 变量；确需调整时先评估所有页面影响。
- 不要在业务样式里大量散落新的硬编码颜色、圆角和阴影。
- 不要为了“高级感”降低信息密度、隐藏金额或弱化高频操作入口。
- 不要新增不必要的 UI 框架、图标库或字体资源。
- 不要让文本、金额、按钮文案在手机宽度下溢出或相互遮挡。

## 11. 新增页面检查清单

开发新增页面或大幅调整页面前，先检查：

- 是否复用了 `.page` 或 `.page-new`。
- 是否使用 `var(--ww-theme-color)`、`bg-primary`、`text-primary` 表达主色。
- 是否优先使用 Ant Design Mobile 和项目已有业务组件。
- 是否为顶部固定栏、底部 TabBar 或底部操作区预留空间。
- 列表行、金额、日期、分类和操作入口是否能在手机宽度快速扫读。
- 空状态、加载态、错误态和禁用态是否完整。
- 是否复用了 Version 10 的 18px 圆角、两级浅阴影和限定的柔和渐变，避免营销式 Hero 和任意装饰色。
- 变更文件是否按项目规范执行对应 ESLint 命令。
